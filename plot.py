import matplotlib.pyplot as plt
import numpy as np
import data

# Display a random matrix with a specified figure number and a grayscale
# colormap
plt.matshow(data.read)
plt.matshow(data.write)
plt.matshow(data.inputs)
plt.matshow(data.prediction)
plt.show()
